import { WorkflowPage } from "@/components/WorkflowPage";
export default async function FinalizePage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="finalize" reportId={id} eyebrow="Function 09 · finalize_report" title="Apply the ruling exactly once." description="Finalization updates the community bond ledger, compensation, treasury fee, and violation count. It cannot be replayed." steps={["Confirm no appeal is pending and the ruling is finalizable.","Review the proposed verdict, fine, and bond capacity on the case page.","Finalize once and verify the transaction on Studio Explorer."]}/>}
