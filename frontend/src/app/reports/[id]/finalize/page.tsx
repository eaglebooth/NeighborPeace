import { WorkflowPage } from "@/components/WorkflowPage";
export default async function FinalizePage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="finalize" reportId={id} eyebrow="Function 09 / finalize_report" title="Transfer the ruling exactly once." description="Finalization deducts the real GEN bond and emits compensation and treasury transfers. It cannot be replayed." steps={["Confirm no appeal is pending and the ruling is finalizable.","Review the proposed GEN fine and remaining bond.","Finalize once, then inspect the parent and triggered transfer transactions."]}/>}
