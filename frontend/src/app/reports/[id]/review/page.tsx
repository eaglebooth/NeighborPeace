import { WorkflowPage } from "@/components/WorkflowPage";
export default async function ReviewPage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="review" reportId={id} eyebrow="Function 06 · evaluate_report" title="Ask the jury to compare both sides." description="GenLayer validators read the available web evidence and reach semantic consensus on attribution, verdict, severity, and confidence." steps={["Check that the case is READY_FOR_REVIEW.","Confirm both evidence URLs are public and stable.","Trigger review and wait for the finalized consensus receipt."]}/>}
