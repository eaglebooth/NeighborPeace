import { WorkflowPage } from "@/components/WorkflowPage";
export default async function AppealPage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="appeal" reportId={id} eyebrow="Function 07 · appeal_report" title="Add new evidence once." description="Either case party may open one appeal after the first ruling. An appeal needs genuinely new, publicly readable evidence." steps={["Confirm the case has a RULING_PROPOSED status.","Connect either the reporter or accused unit owner wallet.","Submit the new evidence URL; each case gets one appeal."]}/>}
