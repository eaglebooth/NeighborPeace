import { WorkflowPage } from "@/components/WorkflowPage";
export default async function AppealReviewPage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="appeal-review" reportId={id} eyebrow="Function 08 · evaluate_appeal" title="Reconsider the ruling, not the wording." description="The appeal jury compares the new evidence against the original verdict and records whether the substantive outcome changes." steps={["Verify the appeal evidence URL still resolves.","Confirm the case status is APPEAL_PENDING.","Trigger the appeal jury and wait for final consensus."]}/>}
