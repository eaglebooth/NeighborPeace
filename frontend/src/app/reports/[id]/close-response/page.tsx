import { WorkflowPage } from "@/components/WorkflowPage";
export default async function ClosePage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="close" reportId={id} eyebrow="Function 05 · close_response_window" title="Move an unanswered case forward." description="The reporter may explicitly close the response window. This records that review proceeds without counter-evidence." steps={["Verify that the case is still AWAITING_RESPONSE.","Connect the reporting unit's registered wallet.","Close the window; no verdict or settlement occurs yet."]}/>}
