import { WorkflowPage } from "@/components/WorkflowPage";
export default async function ClosePage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="close" reportId={id} eyebrow="Function 05 / close_response_window" title="Close an unanswered response stage." description="The accused unit may waive immediately. After the 24-hour deadline, the authenticated reporter may advance an unanswered case so it cannot remain stuck forever." steps={["Verify that the case is still AWAITING_RESPONSE.","Connect the accused unit, or the reporter after the displayed deadline.","Close the response stage; no verdict or settlement occurs yet."]}/>}
