import { WorkflowPage } from "@/components/WorkflowPage";
export default async function ClosePage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="close" reportId={id} eyebrow="Function 05 / close_response_window" title="Waive your right to counter-evidence." description="Only the accused unit may waive its own response. The reporter cannot close this stage unilaterally." steps={["Verify that the case is still AWAITING_RESPONSE.","Connect the accused unit's registered wallet.","Waive the response; no verdict or settlement occurs yet."]}/>}
