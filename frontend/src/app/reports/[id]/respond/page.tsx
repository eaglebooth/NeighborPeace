import { WorkflowPage } from "@/components/WorkflowPage";
export default async function RespondPage({params}:{params:Promise<{id:string}>}){const {id}=await params;return <WorkflowPage mode="respond" reportId={id} eyebrow="Function 04 · submit_response" title="Put the other side on record." description="The accused unit owner can submit a stable counter-evidence URL before the response window closes." steps={["Confirm the case ID and connect the accused unit's registered wallet.","Publish counter-evidence at a URL all validators can read.","Sign once; the case becomes READY_FOR_REVIEW."]}/>}
